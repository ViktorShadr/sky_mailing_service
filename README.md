# Сервис управления рассылками

## О проекте
Сервис управления рассылками - это веб-приложение на Django, предназначенное для создания и управления email-рассылками. Проект разработан в рамках курсовой работы SkyPro (часть 1 + часть 2) и предоставляет функционал для работы с клиентами, сообщениями и рассылками.

## Основные возможности
- 📧 Управление клиентами (добавление, редактирование, удаление)
- ✉️ Создание и управление шаблонами сообщений
- 📊 Управление рассылками (планирование, запуск, мониторинг)
- 📈 Просмотр статистики по рассылкам
- 🔐 Аутентификация и авторизация пользователей
- 👥 Разделение прав доступа (пользователи и менеджеры)
- 🔄 Кеширование для повышения производительности
- 📝 Логирование всех операций

## Требования
- Python >= 3.13, < 4.0
- Django >= 5.2.8, < 6.0.0
- PostgreSQL (рекомендуется) или SQLite
- Redis (опционально, для кеширования)
- Библиотеки (устанавливаются автоматически через Poetry):
  - python-dotenv
  - psycopg2-binary
  - pillow
  - django-redis (опционально)

## Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <ваш-репозиторий>
cd sky_mailing_service
```

### 2. Создание и активация виртуального окружения
```bash
python -m venv venv
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install poetry
poetry install
```

### 4. Настройка переменных окружения
Создайте файл `.env` на основе `.env.example` и настройте параметры:
```ini
# Базовые настройки
SECRET_KEY=ваш_секретный_ключ
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Настройки БД
NAME=sky_mailing
USER=postgres
PASSWORD=ваш_пароль
HOST=localhost
PORT=5432

# Настройки почты
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=ваш_email@gmail.com
EMAIL_HOST_PASSWORD=ваш_пароль_приложения
DEFAULT_FROM_EMAIL=ваш_email@gmail.com

# Redis (опционально)
REDIS_URL=redis://localhost:6379/0
```

### 5. Применение миграций
```bash
python manage.py fill_data
python manage.py makemigrations
python manage.py migrate
```

### 6. Создание суперпользователя
```bash
python manage.py createsu
```
### 7. Создание менеджера
```bash
python manage.py create_manager
```

### 8. Создание группы "Менеджеры"
```bash
python manage.py create_managers_group
```

### 9. Запуск сервера разработки
```bash
python manage.py runserver
```

## Модели данных

### Пользователь (User)
- Кастомная модель пользователя с email в качестве идентификатора
- Поддержка аватарок, телефона и страны
- Разделение на обычных пользователей и менеджеров

### Клиент (Client)
- Email, имя и комментарий
- Привязка к владельцу

### Сообщение (Message)
- Тема и тело сообщения
- Привязка к владельцу

### Рассылка (Mailing)
- Временные рамки (начало и конец)
- Статус (создана, запущена, завершена)
- Связь с сообщением (один-ко-многим)
- Связь с клиентами (многие-ко-многим)

### Лог рассылки (MailingLog)
- Время попытки
- Статус (успешно/неуспешно)
- Ответ сервера
- Связи с рассылкой и клиентом

## Роли и права доступа

### Обычный пользователь
- CRUD для своих клиентов, сообщений и рассылок
- Просмотр статистики по своим рассылкам
- Запуск/остановка своих рассылок

### Менеджер
- Просмотр всех рассылок и клиентов
- Просмотр всех пользователей
- Отключение любых рассылок
- Блокировка пользователей

## Логирование

В проекте реализовано комплексное логирование всех важных событий, что позволяет отслеживать работу приложения и оперативно выявлять проблемы.

### Настройка логирования

Логирование настраивается в файле `config/settings.py` и включает:

1. **Формат логов**:
   - Уровень логирования
   - Временная метка
   - Имя логгера
   - Сообщение

2. **Обработчики (handlers)**:
   - Консольный вывод (для разработки)
   - Файловый вывод с ротацией (максимальный размер файла 5 МБ, хранится 5 бэкапов)

3. **Уровни логирования**:
   - По умолчанию: INFO
   - Для приложения mailing: INFO

### Что логируется

#### Аутентификация пользователей
- Успешные и неудачные попытки входа
- Выход пользователей из системы
- Успешная регистрация новых пользователей
- Ошибки при отправке писем подтверждения регистрации

#### Управление рассылками
- Запуск и остановка рассылок
- Изменение статусов рассылок
- Ошибки при работе с рассылками
- Попытки несанкционированного доступа к рассылкам

#### Отправка писем
- Успешная отправка писем
- Ошибки при отправке
- Количество получателей в рассылке

#### Команды управления
- Запуск и завершение management-команд
- Количество обработанных рассылок
- Ошибки при выполнении команд

### Просмотр логов

Логи сохраняются в файл `logs/app.log` с ротацией. Для просмотра логов в реальном времени можно использовать команду:

```bash
tail -f logs/app.log
```

Примеры записей в логе:
```
INFO 2023-12-09 14:30:45,123 users.views.user Пользователь успешно авторизовался: id=1, email=user@example.com
WARNING 2023-12-09 14:31:10,456 users.views.user Неуспешная попытка входа: email=wrong@example.com
INFO 2023-12-09 14:35:22,789 mailing.services Запуск рассылки id=42
INFO 2023-12-09 14:35:23,001 mailing.services Рассылка id=42: найдено 150 получателей
```

## Кеширование
- Используется Redis при наличии настроек REDIS_URL
- В противном случае используется локальное кеширование в памяти
- Кешируются:
  - Статистика по рассылкам
  - Списки рассылок
  - Другие часто запрашиваемые данные

## Запуск рассылок

1. **Ручной запуск**:
   - Через кнопку в интерфейсе
   - Через админ-панель

2. **Логирование**:
   - Все попытки отправки логируются
   - Доступна история отправок по каждой рассылке

## Команды управления
- `python manage.py send_mailings` - Запуск отложенных рассылок


## Структура проекта
```
sky_mailing_service/
├── config/              # Конфигурация проекта
├── mailing/             # Приложение рассылок
│   ├── management/      # Команды управления
│   ├── migrations/      # Миграции
│   ├── templates/       # Шаблоны
│   ├── views/           # Представления
│   ├── forms.py         # Формы
│   ├── models.py        # Модели
│   └── services.py      # Бизнес-логика
├── users/               # Приложение пользователей
│   ├── migrations/
│   ├── templates/
│   └── models.py
├── static/              # Статические файлы
├── templates/           # Базовые шаблоны
├── .env.example         # Пример настроек
├── manage.py            # Управление проектом
└── pyproject.toml       # Зависимости
```

## Планы по развитию
- [ ] Добавление API для интеграции с внешними сервисами
- [ ] Реализация очередей для асинхронной отправки
- [ ] Улучшение интерфейса аналитики
- [ ] Добавление шаблонов писем с визуальным редактором
- [ ] Интеграция с мессенджерами (Telegram, WhatsApp)

## Лицензия
MIT
